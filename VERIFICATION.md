# Verification

How Docksentry v1.74.0 is checked, and what each check actually asserts.

> Generated by `scripts/verify.py` from a real run of the suite — do not edit by hand.
> The suite runs locally, not in CI: six procedures drive a real container runtime, and a hosted runner without one would report skips as if they were coverage. `scripts/verify.py --check` re-runs it and fails if this file no longer describes what the suite does.

**All 74 procedures pass**, 1753 named checks.

## By area

| Area | Procedures | Checks | |
|---|---:|---:|---|
| Registry & update detection | 24 | 466 | `██████████████████████` |
| Self-update | 8 | 116 | `██████████████████████` |
| Containers & recreate | 9 | 143 | `██████████████████████` |
| Monitoring & events | 5 | 166 | `██████████████████████` |
| Multi-host | 7 | 233 | `██████████████████████` |
| Notifications | 13 | 463 | `██████████████████████` |
| Web interface | 8 | 166 | `██████████████████████` |
| **Total** | **74** | **1753** | |

## Not in the automated run

These need something a plain checkout cannot assume. They are listed so their absence from the total above is visible.

- **`scripts/check_layout_widths.py`** — Measures the layout at 1024–3840px in a real browser. Needs a running instance and Docker; skips with a message rather than failing.
- **`scripts/sim_crashloop.py`** — Drives a container into a restart loop to exercise the monitor's crash path end to end. Needs Docker.
- **`scripts/lint_contracts.py`** — Checks the internal call contracts between modules.

## Procedures

### Registry & update detection

<details><summary>✅ <b>auto update all</b> — 4 checks</summary>

AUTO_UPDATE_ALL global auto-update (#45, @NotRetarded).

- AUTO_UPDATE_ALL on: every container auto-updated
- AUTO_UPDATE_ALL on: nothing left as manual
- off: only the opt-in container auto-updated
- off: the rest reported as manual

</details>

<details><summary>✅ <b>check diag</b> — 51 checks</summary>

Registry diagnostics in the update check (#53, @LeeNX).

- gate: DEBUG off leaks no diagnostic line
- gate: DEBUG off still prints the verdict
- gate: debug_log stays empty
- gate: _vdebug prints nothing without DEBUG
- gate: _vdebug prints with DEBUG
- gate: _vdebug feeds debug_log
- digest: local printed in full
- digest: local keeps repo prefix
- digest: no 30-char truncation left
- digest: remote printed in full
- digest: _get_local_digests still returns bare
- env: line present with DEBUG
- env: printed once
- env: mirror named
- env: daemon proxy named
- env: docker info cached process-wide
- env: no mirrors → none
- env: no daemon proxy → none
- env: docker info failure → unknown
- env: our proxy unset → none
- env: our proxy reported
- env: proxy password masked
- env: mask leaves clean URLs alone
- http: digest still returned
- http: request URL logged
- http: status logged
- http: content-type logged
- http: redirect target logged
- http: auth category logged
- auth: token returned
- auth: token never printed
- auth: category printed
- auth: realm printed
- auth: cached for the run (one negotiation)
- auth: rejected token is forgotten
- auth: config credentials named as such
- auth: basic header not printed
- auth: long scope truncated
- resolve: full sweep without DEBUG resolves nothing
- resolve: single-container check resolves
- resolve: single check prints the version without DEBUG
- resolve: single check without DEBUG stays otherwise quiet
- resolve: version named next to the verdict
- resolve: build date named
- resolve: local size named
- resolve: missing label reported
- resolve: pending update still resolves (unchanged)
- resolve: update verdict unchanged
- inspect: prefix kept, junk dropped
- inspect: cached, second call makes no call
- inspect: missing docker → empty, no crash

</details>

<details><summary>✅ <b>check glob only</b> — 7 checks</summary>

A globbed /check or /update scopes the check via `only=` (#53, @LeeNX).

- /check glob: check_all got only=matched
- /check glob: only the matched were looked up
- /check glob: did NOT check db/cache
- /check glob: notified with web-1's update
- /update glob: check_all got only=matched
- /update glob: only the matched were looked up
- /update glob: run_updates got only pending matches

</details>

<details><summary>✅ <b>check resilience</b> — 9 checks</summary>

What a failed check must NOT do (wud#570/#711, wud#116/#419/#945).

- a failing ps raises rather than returning []
- the exception type is specific
- failed checks are remembered
- the write includes the carried-over entries
- the carry-over is keyed on the failures
- an entry found this run is not double-added
- another host's entries survive
- an unverifiable entry survives
- a verified-gone entry is dropped

</details>

<details><summary>✅ <b>check scoped</b> — 19 checks</summary>

Scoped update check — `UpdateChecker.check_all(only={...})`.

- scoped: exactly one remote lookup
- scoped: returns a list
- scoped: reports a's update
- merge: b's entry untouched
- merge: a still pending
- merge: b not re-checked
- up to date: empty return
- up to date: a removed from pending
- up to date: b still there
- unknown name: empty return
- unknown name: no remote lookups
- unknown name: pending unchanged
- full run: checks everything
- full run: returns a's update
- full run: file overwritten wholesale
- missing file: created with the update
- corrupt file: treated as empty
- debug log: reset per run (no trace of b)
- debug log: mentions the scoped container

</details>

<details><summary>✅ <b>checkimages</b> — 9 checks</summary>

/checkimages command + docksentry-selfupdate hint in /check (#2, @famewolf).

- msg: nothing → 'Nothing to clean up'
- msg: nothing → NO reclaimable line
- msg: 20 GB reclaimable formatted
- msg: auto-cleanup OFF hint present
- msg: auto-cleanup ON hint present
- msg: no OFF hint when ON
- msg: small totals fall back to MB
- hint: fires when checker says yes
- hint: silent when checker says no

</details>

<details><summary>✅ <b>cron dow</b> — 16 checks</summary>

Cron day-of-week, and SMTP certificate verification.

- cron 0 9 * * 1
- cron 0 9 * * 5
- cron 0 3 * * 6
- cron 0 3 * * 0
- cron 0 3 * * 7
- cron 0 9 * * 1-5
- cron 0 9 * * 6,0
- cron 0 9 * * 1-7
- cron 0 9 * * *
- the 'Weekly (Mondays)' preset fires on Monday
- the unsafe default this guards against still exists
- the context we pass does verify
- SMTP_SSL is given a context
- starttls is given a context
- verification is on unless asked otherwise
- the escape hatch is named in the error

</details>

<details><summary>✅ <b>dry run</b> — 5 checks</summary>

Logic test for the /check --dry-run preview builder (roadmap dry-run, #2).

- title
- standalone path when compose file missing
- dependents listed for head
- major jump flagged
- nothing-changed framing

</details>

<details><summary>✅ <b>env override</b> — 67 checks</summary>

Test the "settings.json overrules an env var" detection (#53, @LeeNX).

- set + differs -> reported
- set + differs: saved value still wins
- entry carries var name
- entry carries both values
- entry knows the Settings tab
- entry not marked secret
- set + same -> not reported
- unset + saved off-default -> not reported
- unset + saved off-default: saved applies
- BLIND SPOT: env value == default is not reported
- BLIND SPOT: saved value still wins regardless
- MONITOR -> monitor_enabled
- MONITOR_INTERVAL -> monitor_interval_seconds
- MONITOR_ENABLED is not a real var -> ignored
- every mapped var is really read by from_env()
- every persistent key is mapped or knowingly env-less
- no map entry outside PERSISTENT_KEYS
- declared defaults match from_env() fallbacks
- every mapped key has a declared default
- Dockerfile declares persistent vars at all
- Dockerfile does not declare DEBUG
- Dockerfile ENV values all equal our defaults
- Dockerfile env + saved settings -> no lines at all
- Dockerfile env + one real setting -> exactly one line
- Dockerfile env + LeeNX's DEBUG=true -> exactly that one line
- secret web_password: reported
- secret web_password: marked secret
- secret web_password: no values in entry
- secret web_password: env value absent from message
- secret web_password: saved value absent from message
- secret web_password: variable name still named
- secret web_password: saved value still wins
- secret discord_webhook: reported
- secret discord_webhook: marked secret
- secret discord_webhook: no values in entry
- secret discord_webhook: env value absent from message
- secret discord_webhook: saved value absent from message
- secret discord_webhook: variable name still named
- secret discord_webhook: saved value still wins
- secret webhook_url: reported
- secret webhook_url: marked secret
- secret webhook_url: no values in entry
- secret webhook_url: env value absent from message
- secret webhook_url: saved value absent from message
- secret webhook_url: variable name still named
- secret webhook_url: saved value still wins
- telegram_topic_id treated as secret
- unknown key defaults to secret
- allow-list stays inside PERSISTENT_KEYS
- no secret key on the allow-list
- line: one line per override
- line: names the variable and its env value
- line: names the saved value
- line: says where to change it
- line: names settings.json
- line: docker_stop_timeout points at Settings › Updates
- tab map: web_password -> General
- tab map: healthcheck_max_starting -> Updates
- tab map: docker_stop_timeout -> Updates
- tab map: monitor_enabled -> Notifications
- tab map: monitor_interval_seconds -> Notifications
- multiple overrides reported together
- list value that matches is not reported
- string values rendered as-is
- saved value == default is still an override
- no settings.json -> nothing reported
- no settings.json -> env applies

</details>

<details><summary>✅ <b>glob</b> — 7 checks</summary>

Test glob matching for <name> commands (#40, @LeeNX).

- _is_glob true for *,?,[
- _is_glob false for plain name
- glob *-even matches both even
- glob ctf-* matches all three
- _old leftover excluded
- ? single-char glob
- case-insensitive match

</details>

<details><summary>✅ <b>image age</b> — 13 checks</summary>

Hold an update back until the image has been public for N days.

- off by default
- a fresh image is held
- an aged image passes
- the boundary is inclusive
- the decision carries age and requirement
- a label raises the bar
- a label lowers it too
- a label of 0 disables it
- a nonsense label falls back to the global
- no date, no hold
- a malformed date does not hold
- a missing key does not hold
- a failing label read falls back to the global

</details>

<details><summary>✅ <b>image inherit</b> — 36 checks</summary>

Image-inherited config must not be pinned onto the new image (#35).

- env: inherited APP_VERSION dropped
- env: user TZ kept
- label: inherited OCI version dropped
- label: inherited vendor dropped
- label: compose label kept
- user: inherited not pinned
- workdir: inherited not pinned
- stopsignal: inherited not pinned
- healthcheck: inherited not pinned
- healthcheck: no interval either
- sticky: stale OCI version dropped despite mismatch
- sticky: compose label still kept
- override: user kept
- override: workdir kept
- override: stopsignal kept
- override: healthcheck kept
- override: --no-healthcheck kept
- ns: podman default 'private' dropped
- ns: real 'host' mode kept
- ulimit: podman RLIMIT_NOFILE -> nofile
- ulimit: docker short name untouched
- ulimit: no raw RLIMIT_ passes through
- swappiness: cgroup v2 -> flag suppressed
- swappiness: cgroup v1 -> flag emitted
- swappiness: default/None -> flag emitted (no regression)
- swappiness: -1 never emitted (v1)
- swappiness: -1 never emitted (v2)
- None: replicates env
- None: replicates labels
- None: replicates user
- None: replicates healthcheck
- real: image LABEL lands in container Config
- real: image USER lands in container Config
- real: image WORKDIR lands in container Config
- real: image HEALTHCHECK lands in container Config
- real: filter drops every inherited field

</details>

<details><summary>✅ <b>image ref</b> — 17 checks</summary>

Image-reference parsing and platform selection (cross-tool audit).

- hub digest pin -> skipped
- ghcr digest pin -> skipped
- tag+digest pin -> skipped
- bare image ID -> skipped
- bare official
- user/repo:tag
- ghcr
- port registry
- localhost registry
- platform: each host gets its own
- platform: amd64 host reads amd64
- platform: arm64 host reads arm64
- platform: still cached per host
- platform: silent daemon falls back
- platform is (os, arch) pair
- platform cached (same object)
- platform matches daemon

</details>

<details><summary>✅ <b>labels</b> — 49 checks</summary>

Test container-label config overrides (#42, #52, @LeeNX) and the `-?` help alias (#15, @LeeNX).

- parse: basic
- parse: empty/None -> {}
- bool: true variants
- bool: false value
- bool: absent -> None
- bool: empty/None -> None
- exclude: enable=false
- exclude: exclude=true
- exclude: enable=true -> kept
- exclude: no labels -> kept
- protect: label true overrides toggle off
- protect: label false overrides toggle on
- protect: no label -> toggle (on)
- protect: no label -> toggle (off)
- protect: inspect failure -> toggle (on, never unprotect)
- pin: label true -> skipped
- pin: label false -> kept
- pin: absent -> stored pin rules
- auto: label false beats AUTO_UPDATE_ALL
- auto: label true beats missing toggle
- auto: no label + all_auto
- auto: no label + toggle
- auto: no label + nothing -> manual
- trust: label true
- trust: label false
- trust: absent -> stored (off)
- ask-major: label true forces gate
- ask-major: label false skips gate
- ask-major: absent -> stored list
- link: label beats stored + OCI + heuristic
- link: no label -> stored value
- link: empty label -> stored value (not cleared)
- link: whitespace-only label -> stored value
- link: unsafe label (javascript:) -> stored value
- link: unsafe label (no host) -> stored value
- link: inspect failure -> stored value
- link: stored beats image.source
- link: no label + no stored -> image.source
- link: image.url when no source label
- link: registry heuristic is last resort
- link: nothing anywhere -> ('', 'none')
- link: comma in query survives
- link: case preserved
- link: resolve_container_link returns the url only
- alias: /protect -? -> /help protect
- alias: /status -? -> /help status
- alias: plain command unchanged
- alias: -? not sole arg unchanged
- alias: command with arg unchanged

</details>

<details><summary>✅ <b>manual update gates</b> — 4 checks</summary>

Test that the MANUAL update path (run_updates) honours the same group gates as the auto path (#2, @famewolf — the two had drifted).

- head was attempted first (group order)
- dep1 skipped after head failed (group_aborted)
- dep2 skipped after head failed (group_aborted)
- only the failed head was actually updated

</details>

<details><summary>✅ <b>monitor only</b> — 18 checks</summary>

Monitor-only containers, and wildcard name matching (#55, @LeeNX).

- prefix pattern matches
- prefix pattern does not over-match
- several patterns
- a plain name still works
- a plain name is not a prefix
- empty list matches nothing
- blank entries are ignored
- matching is case-sensitive
- ? and [] work
- pattern makes a container monitor-only
- an unmatched container is not
- label opts a container in
- label opts a matched container OUT
- an unrelated label changes nothing
- update is refused
- the refusal says why
- the refusal is recorded in history
- a label-read failure still refuses

</details>

<details><summary>✅ <b>private ca</b> — 9 checks</summary>

A registry behind the operator's own CA (wud#604, wud#111, wud#52).

- a verify failure names SSL_CERT_FILE
- …and warns off INSECURE_REGISTRIES
- an unrelated TLS error keeps its own message
- HTTP 429 unchanged
- HTTP 401 unchanged
- HTTP 404 unchanged
- a private-CA host is not implicitly insecure
- only a named host goes plain
- a hashed CA directory exists to fall back on

</details>

<details><summary>✅ <b>registry auth</b> — 27 checks</summary>

Registry credential matching, and compose files behind one label.

- hub alias: docker.io
- hub alias: index.docker.io
- hub alias: registry-1.docker.io
- hub alias: https://index.docker.io/v1/
- hub alias: registry.hub.docker.com
- scheme is stripped
- path is stripped
- case is folded
- eu.gcr.io is not gcr.io
- own registry still authenticates
- gcr.io does NOT get eu.gcr.io's secret
- example.com does NOT get the private registry's secret
- an unrelated registry gets nothing
- docker hub resolves through its aliases
- docker.io resolves too
- credsStore-style empty entry yields nothing
- single file is unchanged
- two files are split
- order is preserved
- whitespace around commas survives
- empty label yields nothing
- a comma inside a real path is not split
- a partly-missing split is not trusted
- read-only bind keeps its propagation
- writable bind keeps its propagation
- rprivate is not emitted
- volumes are unaffected

</details>

<details><summary>✅ <b>registry reach</b> — 30 checks</summary>

Registries Docksentry could not reach at all, and Discord's field cap.

- https by default
- http only when named
- an unnamed host stays https
- patterns work
- there is no automatic http fallback
- a Basic challenge is answered
- the auth kind is recorded
- no credentials, no header
- the return value is a full header
- 3 updates -> 1 message(s)
- 3 updates: no embed over 25
- 3 updates: none lost
- 25 updates -> 1 message(s)
- 25 updates: no embed over 25
- 25 updates: none lost
- 26 updates -> 2 message(s)
- 26 updates: no embed over 25
- 26 updates: none lost
- 60 updates -> 3 message(s)
- 60 updates: no embed over 25
- 60 updates: none lost
- a single message has no part number
- a pair is parsed
- a malformed entry is dropped
- a half-empty entry is dropped
- hub alias reaches the canonical host
- an exact host matches
- an unmapped host is untouched
- no map, no change
- the pull side is explicitly out of scope

</details>

<details><summary>✅ <b>remote version</b> — 10 checks</summary>

Old→new version in the "Updates Available" notification (#44, @LeeNX).

- badge: old → new arrow
- badge: only old (current)
- badge: only new
- badge: equal collapses to one
- badge: none → empty
- single-arch: version + created
- multi-arch: picks amd64 + extracts version
- registry failure → {}
- empty remote digest → no update
- differing remote digest → one update

</details>

<details><summary>✅ <b>running image check</b> — 13 checks</summary>

Running-image update check (#53, @LeeNX).

- normal up-to-date: no update (unchanged)
- normal update: one update (unchanged)
- divergence: UPDATE AVAILABLE (not 'up to date')
- digestless running img, tag current: no false update
- digestless running img, tag stale: fallback still reports
- unreadable running id: falls back, no crash, no false update
- no digests anywhere: skipped, no false update
- digestless, run_id==tag_id: no false update (fallback)
- digestless running + digestless tag, ids differ: no false update
- #53 gitea-runner: UPDATE AVAILABLE
- #53 gitea-runner: old_version is the running 2.0.1
- #53 gitea-runner: new_version is the remote 2.3.0
- #53 gitea-runner: divergence spelled out in the log

</details>

<details><summary>✅ <b>tag pagination</b> — 12 checks</summary>

Registry tag pagination (diun#43, #518, #653).

- a path is joined onto the host
- no header, no next page
- no rel=next, no next page
- a malformed header stops rather than guesses
- a relative path without a slash is refused
- same-host absolute url is followed
- a cross-host absolute url is refused
- a missing header object is safe
- the crawl is bounded
- a repeated url breaks the loop
- results are cached per run
- a failed page keeps what was fetched

</details>

<details><summary>✅ <b>trust running</b> — 2 checks</summary>

Regression test for the per-container "trust running state over healthcheck" flag (#9).

- running+unhealthy: OFF→unhealthy, ON→healthy
- crash-loop with flag ON still reported as crashloop

</details>

<details><summary>✅ <b>update policy</b> — 32 checks</summary>

Per-container update policy (v1.53.0, roadmap #2 — @NotRetarded/@famewolf).

- [bump] major 1.2.3 -> 2.0.0
- [bump] minor 1.2.3 -> 1.3.0
- [bump] patch 1.2.3 -> 1.2.4
- [bump] equal -> None
- [bump] unparseable -> None
- [bump] empty old -> None
- [bump] empty new -> None
- [bump] both empty -> None
- [bump] prefix v1.2.3 -> v1.3.0 = minor
- [bump] prefix mixed v1.2.3 -> 2.0.0 = major
- [matrix] policy=all level=major -> allow
- [matrix] policy=all level=minor -> allow
- [matrix] policy=all level=patch -> allow
- [matrix] policy=all level=None -> allow
- [matrix] policy=minor level=major -> block
- [matrix] policy=minor level=minor -> allow
- [matrix] policy=minor level=patch -> allow
- [matrix] policy=minor level=None -> allow
- [matrix] policy=patch level=major -> block
- [matrix] policy=patch level=minor -> block
- [matrix] policy=patch level=patch -> allow
- [matrix] policy=patch level=None -> allow
- [precedence] label minor overrides env all
- [precedence] label all overrides env minor
- [precedence] no label falls back to env patch
- [precedence] invalid label -> all (fail-open)
- [precedence] invalid env -> all (fail-open)
- [auto] minor bump applied under policy=minor
- [auto] major bump NOT applied under policy=minor
- [auto] major bump reported as policy-held
- [auto] policy-held update surfaced in updates-available
- [manual] major bump applied on manual path (policy bypassed)

</details>

### Self-update

<details><summary>✅ <b>release link</b> — 25 checks</summary>

Auto-detected GitHub/GitLab repo links point at the releases page (#52, @LeeNX).

- github bare repo → releases/latest
- github trailing slash
- github .git suffix
- github http (not https) still rewritten
- github www. host
- gitlab bare repo → /-/releases
- deeper/1-seg left alone: https://github.com/amayer1983/docksentry/release
- deeper/1-seg left alone: https://github.com/amayer1983/docksentry/tree/ma
- deeper/1-seg left alone: https://github.com/amayer1983/docksentry/blob/ma
- deeper/1-seg left alone: https://github.com/amayer1983
- deeper/1-seg left alone: https://gitlab.com/group/subgroup/app
- other/invalid left alone: 'https://vaultwarden.example.com'
- other/invalid left alone: 'https://hub.docker.com/_/redis'
- other/invalid left alone: 'https://example.com/owner/repo'
- other/invalid left alone: 'ftp://github.com/owner/repo'
- other/invalid left alone: 'not a url'
- other/invalid left alone: ''
- idempotent on an already-rewritten url
- gitea.com bare repo -> releases/latest
- codeberg (forgejo) rewritten
- self-hosted git.example.com rewritten
- a host merely containing 'gitea' rewritten
- not a forge, left alone: digit.example.com
- not a forge, left alone: legit.io
- gitea deep link left alone

</details>

<details><summary>✅ <b>self detection</b> — 3 checks</summary>

Robust self-container detection (#41, @NotRetarded).

- QNAP: resolves via Config.Hostname fallback
- normal: resolves directly (no scan)
- no match -> empty string

</details>

<details><summary>✅ <b>selfupdate diag</b> — 7 checks</summary>

Self-update failure diagnostics (#43, @LeeNX).

- mount source: resolves /data host path
- mount source: unknown dest → None
- mount source: no mounts → None
- failure: reports on 'rolling back' marker
- failure: log consumed (unlinked)
- success: no report (silent)
- success: log still consumed

</details>

<details><summary>✅ <b>selfupdate marker</b> — 10 checks</summary>

Test for the self-update restart marker (#2, @famewolf).

- fresh marker -> self-update (suppress external-signal line)
- marker consumed (deleted)
- stale marker (>1h) -> ignored
- absent marker -> not a self-update
- _write_selfupdate_marker writes to self.config path
- written marker round-trips through consume -> self-update
- version line shows v_old -> v_new when label readable
- version line omitted when label unreadable
- helper mounts the container's real host socket (podman)
- helper socket falls back to /var/run/docker.sock

</details>

<details><summary>✅ <b>selfupdate queue</b> — 26 checks</summary>

Self-update vs. running container updates (#2, @famewolf).

- queued while batch runs
- queue announced
- body not run while queued
- lock still held by batch
- queued run announced
- queued target passed through
- queue cleared
- second run is a no-op
- body runs when lock free
- lock released after no-update path
- lock kept once swap in flight
- plain selfupdate queues as (None,)
- plain selfupdate dequeues with None
- auto skips while batch runs
- auto inner not called
- auto runs when free
- auto inner called with defer
- auto lock released when no swap
- auto applied
- auto lock kept after swap
- crash: queued selfupdate NOT run
- crash: cancellation announced
- crash: queue cleared
- crash: original exception preserved
- queued-run exception contained
- lock not leaked on exception

</details>

<details><summary>✅ <b>selfupdate recovery</b> — 11 checks</summary>

Self-update recovery net (#43, @LeeNX).

- fail: stop + rename to _old happened
- fail: rolls back (rename _old -> ds + start)
- fail: does NOT rm the _old backup
- ok: recreate + rm _old cleanup ran
- ok: no rollback (no rename back / start)
- quote: backtick label is single-quoted
- quote: $ value is single-quoted
- quote: no command substitution error from sh
- quote: backtick label reaches docker intact
- quote: $ stays literal (not expanded)
- quote: run_parts round-trips via shlex.split

</details>

<details><summary>✅ <b>selfupdate retag</b> — 6 checks</summary>

Logic test for the /selfupdate latest|stable force-re-tag decision (#2).

- latest from pinned tag -> retag
- stable from pinned tag -> retag
- latest while already on latest -> no recreate
- version pin target -> no (digest-differs path handles it)
- None target -> no
- previous target -> no

</details>

<details><summary>✅ <b>web selfupdate row</b> — 28 checks</summary>

Web UI render test for the Docksentry row in the status table (#51).

- auto_selfupdate=True: own row shows the auto badge
- auto_selfupdate=True: own row carries the ⚙ marker
- auto_selfupdate=True: own row does not use the 🏷 label marker
- auto_selfupdate=False: own row shows —
- auto_selfupdate=False: own row still carries the marker
- own row does not post to /api/autoupdate
- own row links to Settings › Updates instead
- docksentry.auto=true on ourselves does not override AUTO_SELFUPDATE=false
- docksentry.auto=false on ourselves does not override AUTO_SELFUPDATE=true
- foreign row keeps the /api/autoupdate toggle
- foreign row has no self marker
- foreign row not auto without opt-in
- foreign row auto via the opt-in list
- foreign row still honours the docksentry.auto label
- empty own_name: no row treated as self
- empty own_name: docksentry falls back to the opt-in list
- empty own_name: nginx falls back to the opt-in list
- _is_own_container: own name -> True
- _is_own_container: other name -> False
- _is_own_container: empty own name -> False for everything
- legend capitalises 'auto'
- legend keeps the lowercase 'auto' in the table header
- legend has no hardcoded English 'major-confirm'
- _legend_word does not mangle 'Auto-Update'
- _legend_word strips the emoji and capitalises
- _strip_emoji leaves the text alone
- stop confirm modal is emoji-free
- stop button tooltip is a full sentence

</details>

### Containers & recreate

<details><summary>✅ <b>container backend</b> — 89 checks</summary>

Container backend argv seam (v2 groundwork).

- argv: ps -q (web _get_containers)
- argv[0]=='docker': ps -q (web _get_containers)
- argv: ps -a --format Names (monitor / web groups all_ids)
- argv[0]=='docker': ps -a --format Names (monitor / web groups all_ids)
- argv: inspect *refs (web _get_containers)
- argv[0]=='docker': inspect *refs (web _get_containers)
- argv: inspect single ref (web detail page)
- argv[0]=='docker': inspect single ref (web detail page)
- argv: inspect --format *refs (web groups_detect)
- argv[0]=='docker': inspect --format *refs (web groups_detect)
- argv: image inspect *imgs (web _get_containers)
- argv[0]=='docker': image inspect *imgs (web _get_containers)
- argv: image inspect --format Size single (web detail page)
- argv[0]=='docker': image inspect --format Size single (web detail page)
- argv: logs --tail N NAME (web detail / logs page)
- argv[0]=='docker': logs --tail N NAME (web detail / logs page)
- argv: logs NAME (no tail)
- argv[0]=='docker': logs NAME (no tail)
- argv: stats --no-stream --format (monitor _memory_snapshot)
- argv[0]=='docker': stats --no-stream --format (monitor _memory_snapshot)
- argv: info --format
- argv[0]=='docker': info --format
- argv: version --format
- argv[0]=='docker': version --format
- argv: system df --format
- argv[0]=='docker': system df --format
- argv: pull
- argv[0]=='docker': pull
- argv: stop --time
- argv[0]=='docker': stop --time
- argv: stop without --time
- argv[0]=='docker': stop without --time
- argv: kill
- argv[0]=='docker': kill
- argv: start
- argv[0]=='docker': start
- argv: rename
- argv[0]=='docker': rename
- argv: rm
- argv[0]=='docker': rm
- argv: rm -f
- argv[0]=='docker': rm -f
- argv: rm multiple
- argv[0]=='docker': rm multiple
- argv: image prune -a --force --filter
- argv[0]=='docker': image prune -a --force --filter
- argv: image prune bare
- argv[0]=='docker': image prune bare
- argv: image save
- argv[0]=='docker': image save
- argv: generic run: compose up
- argv[0]=='docker': generic run: compose up
- run forwards timeout
- run defaults capture_output=True
- run defaults text=True
- text mode: stop is True
- text mode: kill is True
- text mode: start is False
- text mode: rename is False
- text mode: rm is False
- text mode: pull is True
- a call without a timeout still gets one
- an explicit timeout still wins
- remote fails faster than local
- long operations keep their own timeout
- DockerBackend.cli_binary == 'docker'
- get_backend returns a DockerBackend
- PodmanBackend.cli_binary == 'podman'
- argv: podman ps
- argv: podman pull
- argv: podman stop
- argv: podman compose (generic run)
- argv: remote ps
- argv: remote pull
- argv: remote rm -f
- argv: remote compose (generic run)
- remote carries its host name
- remote defaults name to the endpoint
- argv: remote podman uses --url
- argv: remote docker still uses -H
- local backend emits no global args
- local backend name is 'local'
- resolve_cli('podman') → podman
- resolve_cli('docker') → docker
- resolve_cli('auto') picks one of the two
- resolve_cli(junk) falls back to auto
- get_backend(container_cli='podman') → PodmanBackend
- get_backend(container_cli='docker') → DockerBackend
- default_backend() follows get_backend

</details>

<details><summary>✅ <b>cooldown</b> — 9 checks</summary>

Tests for per-container update cooldown (#2).

- set/get roundtrip
- clamp over 600
- clamp negative to 0
- bad value -> 0
- zero clears entry
- unset container -> 0
- sleeps cooldown when more remain
- no sleep when last in batch
- no sleep when cooldown unset

</details>

<details><summary>✅ <b>dependents recreate</b> — 4 checks</summary>

End-to-end test for the recreate-dependents cascade (#8).

- plain restart of sidecar fails after head recreate (the bug)
- recreate_dependent rejoins sidecar to new head
- cascade recreates netns sidecar + restarts plain member
- cascade message reports success (no failures)

</details>

<details><summary>✅ <b>disk guard</b> — 0 checks</summary>

Unit test for the disk-warning in-memory re-send guard (v1.23.6).

- _(this procedure reports only a final verdict)_

</details>

<details><summary>✅ <b>disk reclaim</b> — 13 checks</summary>

Extended disk warning: reclaimable + auto-cleanup hint (#2, @famewolf).

- parse: 20.1GB → ~20.1e9
- parse: 20.1GB (50%) [Docker's real format]
- parse: 2.113MB
- parse: 0B → 0
- parse: empty/junk → 0
- reclaimable ≈ sum of all reclaimable types
- reclaimable: docker error → 0
- reclaimable: no output → 0
- msg: shows GB reclaimable
- msg: warns auto-cleanup OFF
- msg: no auto-cleanup hint when ON
- msg: no reclaim line when 0
- msg: falls back to MB for small totals

</details>

<details><summary>✅ <b>groups cmd</b> — 8 checks</summary>

Logic test for the /groups Telegram command (roadmap, #2).

- list shows both groups
- head crown only on multi-member
- detail running icon
- detail stopped icon
- restart button on restart_dependents group
- no button on single-member group
- partial name resolve
- not-found message

</details>

<details><summary>✅ <b>netns recreate</b> — 4 checks</summary>

End-to-end test for the Gluetun-style netns-sidecar recreate fix (#2).

- netns_target_name resolves head name
- build_run_args uses container:<name> (not stale id)
- stale-id recreate fails (reproduces the bug)
- name-override recreate succeeds + joins new head

</details>

<details><summary>✅ <b>op coordination</b> — 12 checks</summary>

Operation coordination under the shared update mutex (#2, @famewolf).

- cleanup: busy -> (None, busy-msg)
- cleanup: prune NOT run while busy
- cleanup: batch lock untouched
- cleanup: runs when free
- cleanup: exactly one prune
- cleanup: lock released after
- cleanup: mutex held during prune
- queued selfupdate runs after cleanup
- lifecycle: stop refused while updating
- lifecycle: restart refused while updating
- lifecycle: start refused while updating
- lifecycle: passes guard when free

</details>

<details><summary>✅ <b>protect stop</b> — 4 checks</summary>

Test for the per-container stop-protection flag (#38, @LeeNX).

- toggle/is roundtrip
- stop refused while protected (container still running)
- restart allowed while protected
- stop works again after unprotect

</details>

### Monitoring & events

<details><summary>✅ <b>event resources</b> — 16 checks</summary>

The event log keeps the memory/CPU picture, not just the exit code (#2).

- oom collects the picture
- crash_restart collects the picture
- exited collects the picture
- recovered collects nothing
- unhealthy collects nothing
- the event is written
- resources ride along
- detail keeps its own meaning
- no empty resources key
- a remote host still records what it can
- an idle CPU is left out
- an old event yields an empty picture
- the renderer reads it defensively
- the row uses monitor_host_memory
- the row uses monitor_top_memory
- the row uses monitor_top_cpu

</details>

<details><summary>✅ <b>event watcher</b> — 32 checks</summary>

Event watcher: parsing, evidence lifetime, burst sharing, reconnect.

- docker die parses
- docker oom parses
- podman died parses as die
- podman clean exit parses
- noise ignored: ''
- noise ignored: 'not json'
- noise ignored: '[]'
- noise ignored: 'null'
- noise ignored: '{"Type":"image","Actio'
- noise ignored: '{"Action":"die"}'
- noise ignored: '{"Action":"start","Act'
- exec_die is not a container death
- evidence recorded on a crash
- evidence is consumed
- stale evidence is dropped
- clean exit records nothing
- clean exit costs no stats call
- die does not erase the oom flag
- saw_oom peeks, does not consume
- exit code comes from the stream
- exit code peeks, does not consume
- no event, no exit code
- podman exit code arrives as an int
- a stack collapsing costs one stats call
- every container in the burst has evidence
- a crash-looping host cannot grow this forever
- a refused stats call still records the death
- a refused stats call does not raise
- a dropped stream is reconnected
- evidence survives the reconnect
- remote watcher keeps the endpoint flag
- podman remote uses --url, not -H

</details>

<details><summary>✅ <b>health output</b> — 15 checks</summary>

An unhealthy alert carries what the probe said (#2, @famewolf).

- a failing probe is reported
- the failing probe wins over a later pass
- a passing probe is used as a fallback
- the newest failure wins
- no healthcheck yields nothing
- an empty log yields nothing
- a silent probe yields nothing
- a missing Health key is safe
- a long output is capped
- …and marked as cut
- the snapshot carries the probe output
- the snapshot keeps its other fields
- the alert reads health_output
- the log tail is still attached
- only health flips look for a probe

</details>

<details><summary>✅ <b>monitor</b> — 96 checks</summary>

Container state monitoring (#2, @NotRetarded).

- memory diagnostics attach to oom
- memory diagnostics attach to crash_restart
- memory diagnostics attach to exited
- a recovery message stays clean
- host memory reads as a human string
- host memory carries no translatable words
- remote monitor reports no host memory
- cpu line names the hog
- memory ranking survives the cpu column
- one stats call feeds both readings
- idle host gets no cpu line
- a burst of events shares one stats call
- cached snapshot still renders
- next tick measures afresh
- crash_restart takes the stream's exit code
- unknown container keeps its old code
- exited is left alone
- no watcher, no change
- the detail dict is not mutated in place
- label on host ''
- label on host 'local'
- label on host 'nas'
- labelling follows container_store
- baselines are per monitor
- diff has no health events
- first unhealthy flip is silent (pending)
- confirmed unhealthy fires on next pass
- confirmed unhealthy does not repeat
- recovery of confirmed fires once
- one-pass flap is fully silent
- no healthcheck -> no health events
- restart-recovery: confirmed first
- restart-recovery: ->starting fires recovered once
- restart-recovery: later episode alerts again
- restart-recovery: ->starting->healthy fires once, no double
- restart-recovery: unconfirmed blip stays silent
- prune: vanished container drops stuck flag
- running unhealthy-at-baseline: no spurious alert
- stopped frozen-unhealthy: no event ever
- stopped frozen-unhealthy: stale flags cleared, no event
- genuine flip: pending, silent
- genuine flip: confirms once with prev=healthy
- genuine flip: does not re-fire
- alerted then stops: no recovered event, flag cleared
- no unhealthy event ever has prev=='unhealthy'
- tick: unhealthy-at-baseline never alerts
- tick: still-unhealthy-at-baseline still silent
- tick: stopped frozen-unhealthy never alerts
- zero exit is silent
- non-zero exit fires with code
- oom exit fires as oom
- restart count increase fires
- count reset (recreate) is silent
- crash loop caught 'restarting' fires crash_restart
- crash loop caught 'exited' + count up fires crash_restart
- crash alert carries the exit code of the run that died
- crash alert carries the restart time
- crash alert still carries the count
- a missing timestamp degrades to empty, not to garbage
- one-shot exit, count unchanged, fires exited
- oom precedence over crash_restart when count up
- vanished container is silent
- new container is baselined silently
- first tick: silent baseline
- health flip pending: no notify yet
- own container ignored
- confirmed unhealthy notifies on next pass
- message rendered via i18n key
- recovery of confirmed notifies once
- one-pass flap: zero notifications
- vanish/reappear: first episode alerts
- vanish/reappear: flag pruned on vanish
- vanish/reappear: second episode alerts again
- exit fires immediately
- oom fires immediately
- crash_restart fires immediately
- tick skipped while updating
- baseline dropped while updating
- post-update tick re-baselines silently
- docksentry.monitor=false opts out
- exclude_containers honored
- oom msg carries memory snapshot
- oom msg carries log tail
- unhealthy msg carries tail, no snapshot
- recovery msg stays clean
- tail failure doesn't break notify
- mem: GiB
- mem: MiB
- mem: decimal MB
- mem: plain bytes
- mem: garbage -> 0
- event: persisted to file
- event: structure complete
- event: capped at MAX_EVENTS
- event: newest survives the cap
- event: write failure doesn't break tick

</details>

<details><summary>✅ <b>podman live</b> — 7 checks</summary>

Podman against a REAL podman, including a remote endpoint (#7, #23).

- local podman answers through the backend
- ps works locally
- podman remote uses --url, not -H
- docker remote still uses -H
- remote podman answers over the emitted flag
- remote ps succeeds
- generic run() reaches the remote too

</details>

### Multi-host

<details><summary>✅ <b>docker hosts</b> — 24 checks</summary>

DOCKER_HOSTS parsing and per-host backends (#7, multi-host groundwork).

- parses name:endpoint pairs
- splits on the first colon only
- tolerates loose whitespace
- empty input → no hosts: ''
- empty input → no hosts: None
- empty input → no hosts: '   '
- empty input → no hosts: ',,'
- missing endpoint skipped
- empty endpoint skipped
- bad name skipped
- name starting with dash skipped
- good entries survive a bad neighbour
- names lowercased
- duplicate names dropped
- first duplicate wins
- reserved name rejected: local
- reserved name rejected: all
- 'all' is reserved
- checker keeps the backend it was given
- checker's commands carry the host endpoint
- no backend given → local, no endpoint flag
- nas scan drops only nas entries
- legacy entry (no host key) survives a remote scan
- local scan clears local entries

</details>

<details><summary>✅ <b>host scoped store</b> — 34 checks</summary>

Per-host container state (#7, multi-host).

- local host key is bare
- no host given → bare
- remote host key is prefixed
- round-trip remote
- bare key reads as local
- pin on local doesn't pin on nas
- both hosts can pin the same name
- each host sees only its own
- stored as distinct keys
- unpin on nas leaves local pinned
- bulk save keeps the other host's entries
- bulk save applies to own host
- existing flat data reads as local
- cooldowns are per host
- cooldown listing is per host
- notes are per host
- auto-update is per host
- auto listing is per host
- group members stored host-keyed
- group reads back with plain names
- other host doesn't see the group
- group lookup by member is host-scoped
- group lookup returns plain member names
- get_pinned returns the same type as the store
- get_autoupdate returns the same type as the store
- get_ask_before_major returns the same type as the store
- get_trust_running returns the same type as the store
- get_protect_stop returns the same type as the store
- get_update_windows returns the same type as the store
- get_cooldowns returns the same type as the store
- get_pending_major returns the same type as the store
- get_notes returns the same type as the store
- get_links returns the same type as the store
- get_groups returns the same type as the store

</details>

<details><summary>✅ <b>host state wiring</b> — 44 checks</summary>

Per-host container state is actually WIRED UP (#7).

- pin @nas pins on nas
- pin @nas does NOT pin on local
- pin @nas is stored host-keyed
- a second pin on local leaves nas' pin intact
- both pins coexist on disk
- unpin on local leaves nas pinned
- auto-update toggle is per host
- toggling local auto-update leaves nas on
- toggling nas off leaves local on
- per-host toggle reply names the host
- cooldown is per host
- local cooldown doesn't disturb nas'
- protect-stop is per host
- link override is per host
- link resolver picks the entry's own host override
- unknown host errors and writes nothing
- engine: local recreate ignores nas' cooldown
- engine: nas recreate uses nas' cooldown
- engine: ask-major list is the update's host's
- engine: a local group doesn't order another host's batch
- engine: the local group DOES order the local batch
- auto pass: only nas' web was updated
- auto pass on nas keeps local's pending entry
- auto pass on nas keeps nas' other pending entry
- auto pass on nas dropped exactly its own processed entry
- single update: the host key picked nas' checker
- single update on nas leaves local's same-named entry
- removing (nas, web) leaves (local, web)
- a host-less legacy entry is treated as local
- checker: local sees only unprefixed pins
- checker: nas sees its own pins, unprefixed
- checker: a nameless backend is the local host
- single host: a one-host registry writes the same bytes as none
- single host: a one-host registry sends the same replies as none
- adding a second host doesn't change what local writes
- single host: pinned file is the plain unprefixed list
- single host: autoupdate file is the plain unprefixed list
- single host: cooldown file is the plain unprefixed dict
- single host: protect file is the plain unprefixed list
- single host: links file is the plain unprefixed dict
- single host: no `/` ever reaches a state key
- single host: no reply carries a host marker or hint
- single host (no-registry): auto pass leaves exactly the untouched entry
- single host (one-host): auto pass leaves exactly the untouched entry

</details>

<details><summary>✅ <b>hosts</b> — 30 checks</summary>

The managed-host registry (#7, multi-host).

- no config → exactly one host
- that host is local
- single host is not 'multi'
- local host sends no endpoint flag
- local + configured hosts
- local stays first
- is_multi once there are remotes
- lookup by name
- lookup is case-insensitive
- unknown host → None
- empty name → None
- only local is flagged local
- remote checker carries its endpoint
- second remote uses its own endpoint
- same name pinned on two hosts
- nas untouched by the other two
- local key stays unprefixed on disk
- remote inherits the podman CLI
- no @token → no target
- empty input
- @host alone
- @host after the name
- @host before the name
- @host in the middle
- @all is a distinct target
- @all is not a host named 'all'
- host names are case-normalised
- bare @ is left alone
- only the first @token is consumed
- glob arguments survive

</details>

<details><summary>✅ <b>multihost live</b> — 6 checks</summary>

Multi-host end to end against a REAL container daemon (#7).

- registry has local + the remote
- remote host lists containers over its endpoint
- remote view matches the local one
- pin via remote is visible on remote
- pin via remote is NOT on local
- stored under the host-prefixed key

</details>

<details><summary>✅ <b>multihost routing</b> — 45 checks</summary>

Multi-host command targeting in the Telegram bot (#7).

- single host: hosts=None and a one-host registry reply identically
- single host: no `@host` marker anywhere
- single host: no local-only hint anywhere
- single host: /status is the plain overview
- single host: /update <name> unchanged
- single host: /help has no multi-host block
- single host: unknown container answers as before
- single host: a glob matching nothing answers as before
- single host: `@` argument is not treated as a host
- read /check: both hosts checked
- read /check: remote results carry @nas
- read /check: local results carry no marker
- read /status: containers from both hosts
- read /status: 4 containers total
- read /updates: both hosts listed
- read /updates @nas: only that host
- write /update: only the local host checked
- write /update: only the local host updated
- write /update: reply carries the local-only hint
- write /stop: acted on the local host only
- write /stop: reply carries the local-only hint
- @nas: only the named host was checked
- @nas: the named host was updated
- @nas: no local-only hint when aimed explicitly
- @nas: lifecycle hit the remote host
- @nas: lifecycle reply names the host
- @nas: bare check runs on that host only
- @nas: /status is that host's overview
- one reply for a container only the local host has
- one reply for a container only the remote host has
- remote /status detail carries no lifecycle buttons
- a container on both hosts answers once per host
- local /status detail keeps its buttons
- a name no host has answers once
- @all: every host checked
- @all: every host updated
- @all: no local-only hint
- unknown host: `/check @typo` errors and does nothing
- unknown host: `/update web @typo` errors and does nothing
- unknown host: `/stop web @typo` errors and does nothing
- unknown host: `/status @typo` errors and does nothing
- unknown host: `/updates @typo` errors and does nothing
- /help documents @host and @all
- /help documents the read/write split
- /help lists the managed hosts

</details>

<details><summary>✅ <b>web multihost</b> — 50 checks</summary>

Web UI actions on several hosts (#7).

- single host: a one-host registry renders byte-identically to none
- single host: no host column
- single host: no host filter
- single host: no data-host on any row
- single host: form fields carry the bare container name
- single host: only the LOCAL pending entry lights a row up
- multi: every host's containers are listed
- multi: the host column is back
- multi: the host filter offers every host plus 'all'
- multi: an unreachable host is one line, not a broken page
- multi: a remote row has the same actions as a local one
- multi: every form on a remote row carries that host's key
- multi: a remote row's update button follows that host's pending
- multi: a local row with no pending update has no update button
- no host in the request resolves to the local objects
- an explicit `local` resolves the same way
- a remote key resolves that host's backend and checker
- a remote key resolves that host's store view
- an unmanaged host resolves to nothing — never to local
- an empty name resolves to nothing
- single host: a remote key is refused, not taken as local
- pin on a remote host writes that host's key
- pin on host A does not pin on host B
- pin with no host given pins locally
- unpin on a remote host leaves the local pin alone
- pin naming an unmanaged host changes nothing
- every per-container toggle lands on the named host only
- a remote per-container form returns to the table, not to a local detail URL
- lifecycle on a remote row uses that host's checker AND backend
- lifecycle with no host given stays local
- lifecycle naming an unmanaged host does nothing
- the self-kill guard still refuses a local stop of ourselves
- the self-kill guard does not refuse a same-named remote container
- update on a remote row runs that host's entry through its checker
- update on a remote row drops only that host's pending entry
- update with no host given uses the local entry and local checker
- a mixed batch is one call per host, each with its own checker
- a mixed batch removes both hosts' entries, keyed by host
- the update lock is released again afterwards
- an update naming an unmanaged host runs nothing at all
- a busy update lock blocks the whole batch, not just one host
- bulk pin splits a mixed selection per host
- bulk auto-update-on lands on the named host only
- bulk auto-update-off removes only that host's entry
- check-one on a remote row uses that host's checker
- check-one with no checker given uses the local one
- a deferred major shows up once per host, with its host key
- rejecting a remote major clears that host's entry only
- confirming a major hands the host key to the shared resumer
- the rendered keys ARE container_store.host_key output

</details>

### Notifications

<details><summary>✅ <b>api retry</b> — 4 checks</summary>

Telegram api_call bounded retry on transient network failures (#2, @NotRetarded).

- retries then succeeds (3rd attempt)
- gives up after 3 attempts -> None
- long-poll not retried (1 attempt)
- HTTP 4xx returns body, not retried

</details>

<details><summary>✅ <b>discord bot</b> — 263 checks</summary>

Discord slash commands onto the shared engine (v2.0 Discord bot).

- every command has a name
- every command has a description
- descriptions fit Discord's 100-char limit
- names are lowercase (Discord rejects capitals)
- options are typed
- option names are lowercase
- option descriptions fit the 100-char limit
- command names fit the 32-char limit
- option names fit the 32-char limit
- names use no characters Discord rejects
- command names are unique
- option names are unique within a command
- required options come before optional ones
- option types are ones Discord knows
- …and that check would catch a bad ordering
- interaction is acknowledged immediately
- the ack is dispatched off the gateway loop
- acknowledgement is deferred (buys 15 minutes)
- no host given → every host
- named host → just that one
- unknown host → None, never a fallback
- status lists every host's containers
- remote entries are labelled with their host
- local entries carry no label
- host option narrows the listing
- unknown host is reported, nothing is listed
- …and it names the hosts that do exist
- container option filters
- check finds the pending update
- check backs off while an update runs
- a dead host is reported, the rest still lists
- a long listing is clipped below the 2000 limit
- clipping says it clipped
- single host output carries no host marker
- /hosts says so plainly on a single host
- token without application id is disabled
- both present → enabled
- /pin confirms the pin
- /pin writes to the local host
- /pin with no host does NOT touch the other host
- /pin host:nas pins there
- …and says which host it acted on
- …while the local pin is untouched
- the two pins are separate keys in the raw store
- pinning twice is reported, not duplicated
- /pin on an unknown host errors
- …and changes nothing at all
- …and never falls back to local
- /unpin removes the local pin
- …and leaves the other host pinned
- /unpin confirms
- /unpin of something unpinned says so
- /unpin on an unknown host errors
- …and the real pin survives
- /unpin host:nas clears it there
- /autoupdate turns it on
- /autoupdate does not leak to the other host
- /autoupdate toggles back off
- /autoupdate host:nas targets that host
- …and the local host stays empty
- /autoupdate on an unknown host errors
- /protect turns protection on
- /protect does not leak across hosts
- /protect toggles back off
- /protect host:nas targets that host
- /protect on an unknown host errors
- /cooldown sets the value
- …and says so
- /cooldown does not leak across hosts
- /cooldown reports the CLAMPED value, not the asked-for one
- /cooldown 0 clears it
- /cooldown rejects a non-number
- /cooldown host:nas targets that host
- …and the local one is still clear
- /cooldown on an unknown host errors
- …and writes nothing
- an unknown container is reported
- …and nothing is pinned
- /history with no file says so
- /history lists entries
- /history marks failures
- /history is newest first
- /history filters by container
- /history says when the filter matches nothing
- /events with no file says so
- /events lists events
- /events is newest first
- /events renders the detail payload
- /logs returns the log text
- /logs fences the output
- /logs defaults to 30 lines
- /logs honours the lines option
- /logs clamps an absurd line count
- /logs host:nas reads from that host
- …and the remote log is labelled
- /logs on an unknown host errors
- /logs of an unknown container is reported
- a long log stays under Discord's 2000 limit
- …and its code fence is still closed
- /groups with none configured says so
- /groups lists the group
- /groups shows the members
- /groups marks the leader
- a group is per host — the other host has none
- …and the group carries its host marker
- /settings shows the schedule
- /settings shows excludes
- /settings lists the managed hosts
- /settings stays under the 2000 limit
- /maintenance reports off by default
- /maintenance 2h turns it on
- …and the state is read back
- /maintenance forever has no end time
- /maintenance off turns it off
- /maintenance rejects nonsense
- …and a rejected duration changed nothing
- /update runs the container through the shared batch engine
- /update is a manual update, never the auto path
- /update with no host acts on the LOCAL host only
- /update drops just that host's pending entry
- /update reports the result
- /update host:nas updates there
- …and only that entry leaves the pending file
- /update of a container with no pending update says so
- …and runs nothing
- /update on an unknown host errors
- …and starts no update
- …and leaves the pending file untouched
- /update refuses while the shared lock is held
- …and starts nothing
- …and the pending file is untouched
- …and the refusal did not swallow the lock
- /updateall does not act on the first invocation
- …it hands back a button
- …and previews what it would do
- the button lives in an action row
- the confirm button is styled as dangerous
- custom_id is namespaced to this bot
- custom_id fits Discord's 100-char limit
- /updateall with no host previews only the local host
- the button press runs the update
- …on the local host only
- …clearing only the local pending entry
- a spent confirmation cannot be pressed again
- …and nothing runs the second time
- Cancel changes nothing
- …and burns the token with it
- an unknown token is refused
- a foreign custom_id is not claimed
- …and the action in the custom_id has to match the record
- /updateall on an unknown host errors
- …and offers no button at all
- …and parks no confirmation
- a confirmed /updateall still refuses while the lock is held
- …and updates nothing
- someone else cannot press my confirmation
- …and it runs nothing
- …while the asker still can
- an expired confirmation is refused
- …and an expired one runs nothing
- /stop asks before it acts
- …and stops nothing yet
- …and names what it would stop
- the stop button carries the stop action
- the press stops it
- …and confirms it
- /stop with no host stops only the local container
- /stop host:nas stops there
- /stop on an unknown host errors
- …and offers no confirmation
- …and stops nothing
- /stop refuses a protected container
- …without even offering the button
- …and a protected container is not stopped
- protection set after the question is still honoured
- a protect LABEL refuses the stop too
- /restart restarts
- …with a graceful timeout
- /start starts
- /restart host:nas acts there only
- /restart on an unknown host errors
- …and touches no container
- /restart is refused while an update runs
- …and issues no command
- /start is refused too
- /restart refuses to restart Docksentry itself
- /stop refuses to stop Docksentry itself
- /cleanup runs the prune
- …and reports what it freed
- /cleanup skips while the update lock is held
- …and prunes nothing
- …and left the lock alone
- /cleanup never touches another host
- /checkimages is a read — it spans every host
- …and points at /cleanup when there is something to free
- /checkimages host:nas answers for that host
- /checkimages says so when there is nothing to reclaim
- /checkimages on an unknown host errors
- a fresh interaction is answered by editing it
- an answer past the 15-minute window is not edited
- …it is posted into the channel instead
- …addressed to whoever asked
- …carrying the result itself
- …and explaining why it is not ephemeral
- a rejected edit falls back to the channel
- an answer with no channel to fall back to is logged
- the channel fallback respects the 2000-char limit
- a slow command warns before it starts
- …and still delivers the real answer afterwards
- a button press is acknowledged immediately
- …deferred, like a command
- another bot's button is left alone
- an interaction type we don't handle is ignored
- a queued self-update is handed on after the lock is released
- …and the lock really was released
- the worker pool has a small, positive bound
- the pool refuses work once every slot is taken
- an over-limit interaction is answered, not silently dropped
- …and told plainly that nothing was started
- …with an immediate reply, not a deferred promise it can't keep
- …and no command ran
- a finished worker hands its slot back
- stop() waits for a command that is still running
- …and really waited rather than getting lucky
- …while the wait is still bounded
- a transient 500 does not publish a private answer
- …and it is logged rather than lost without trace
- a 429 does not publish one either
- an expired token still falls back to the channel
- …and so does a webhook Discord no longer knows
- a 404 for anything else is not treated as an expiry
- /updateall updates only the containers it previewed
- …leaving what turned up afterwards pending
- …and saying that it did not touch them
- /updateall says so when the approved set shrank
- …and updates what is left of it
- /updateall with nothing approved left runs nothing
- …and explains that, rather than claiming nothing was pending
- …and leaves the unapproved entry alone
- a press we cannot attribute to anyone is refused
- …and an unattributable press runs nothing
- …while a failed identity check leaves the button pressable
- a confirmation with no identified owner is not pressable
- …and it too runs nothing
- single host: no command emits a host marker
- single host: state still lands in the store
- single host: keys stay unprefixed
- every COMMANDS entry has a handler
- an unknown command still says so
- no reply exceeds Discord's 2000-character limit
- auth: an interaction from the configured guild passes
- auth: another guild is refused
- auth: a missing guild (DM) is refused
- auth: no DISCORD_GUILD_ID → nothing is authorised
- auth: …and the bot refuses to start at all
- auth: a listed user passes
- auth: an unlisted user is refused
- auth: an unidentifiable user is refused when a list is set
- auth: an empty list means anyone in the guild
- auth: an unauthorised interaction gets no reply at all
- auth: every command is admin-only by default
- auth: no command is usable in a DM
- auth: hardening does not mutate COMMANDS

</details>

<details><summary>✅ <b>discord gateway</b> — 35 checks</summary>

The Discord gateway client, against a fake gateway (v2.0 Discord bot).

- sends IDENTIFY after HELLO
- IDENTIFY carries the token
- IDENTIFY declares intents
- sends heartbeats
- heartbeat carries the sequence
- READY is surfaced
- session id is remembered
- dispatch reaches the handler
- first connection identifies
- after RECONNECT it RESUMEs
- RESUME carries the session
- RESUME carries the sequence
- an invalidated session re-IDENTIFYs, never RESUMEs
- an unacknowledged heartbeat forces a reconnect
- bad token (4004) is fatal
- disallowed intents (4014) is fatal
- a normal close (1000) is retryable
- rate limited (4008) is retryable
- a 4004 close ends run_forever instead of reconnecting
- …and it never re-IDENTIFYs on a rejected token
- …and it says why, naming the token
- a dropped session is RESUMEd once
- a RESUME rejected with 4009 is not RESUMEd again
- …and the dead session id is forgotten
- close 4007 clears the session so the next attempt identifies
- close 4009 clears the session so the next attempt identifies
- a disallowed-intents close raises the fatal error
- …while an ordinary close is only a reconnect
- three healthy connections do not compound the backoff
- a connection that never reached READY is not 'healthy'
- …one that stayed up past the threshold is
- a ws:// resume URL is refused
- …and so is any other scheme
- a wss:// resume URL is honoured
- no resume URL at all falls back to the configured gateway

</details>

<details><summary>✅ <b>discord rest</b> — 23 checks</summary>

Discord REST client, against a local fake API (v2.0 Discord bot).

- GET /users/@me returns the body
- sends the bot token
- sends a descriptive User-Agent
- uses the right path
- 429 is retried, not raised
- waits exactly the retry_after
- falls back to the Retry-After header
- an absurd retry_after is refused
- …and it did not sleep on it
- 401 raises
- 401 is not retried
- 5xx is retried
- guild commands use the guild path
- registration is a bulk PUT
- global commands use the global path
- immediate response is type 4
- response carries the content
- responses are ephemeral by default
- 204 decodes to None
- deferred response is type 5
- editing the original is a PATCH
- edit targets @original
- messages post to the channel

</details>

<details><summary>✅ <b>edited message</b> — 6 checks</summary>

Recalled-command handling (#15, @famewolf).

- normal message passes through
- recent edit (<=120s) honoured
- stale edit (>120s) ignored
- edit with no edit_date ignored
- empty update -> {}
- message wins over a co-present edit

</details>

<details><summary>✅ <b>i18n partial</b> — 9 checks</summary>

A missing placeholder must not discard the whole message (#2, @NotRetarded).

- no raw braces survive
- the container name comes through
- the exit code comes through
- what was never stored is marked
- a partially-complete event keeps its count
- a complete event is untouched
- the ordinary path is unmarked
- all languages recover
- the missing-marker mapping exists

</details>

<details><summary>✅ <b>notifier plugins</b> — 17 checks</summary>

Notifier plugin layer — registry, best-effort dispatch, ntfy request.

- registry: discord only
- registry: smtp only
- registry: discord+webhook, stable order
- registry: nothing configured -> empty
- registry: ntfy discovered from env
- registry: expected channel set
- dispatch: raising channel isolated
- ntfy: POST to topic url
- ntfy: Title header
- ntfy: Priority high on failure
- ntfy: body carries detail
- ntfy: Priority default on success
- ntfy: bot_label prefixes title
- ntfy: server+topic configured
- ntfy: server+topic joined url
- ntfy: message strips bold markers
- ntfy: unset -> not configured

</details>

<details><summary>✅ <b>notifier retry</b> — 6 checks</summary>

Notifier bounded retry — Discord + Webhook (audit follow-up to v1.38.1).

- discord: retries then succeeds on 3rd attempt
- discord: gives up after 3 -> None
- webhook: gives up after 3 -> None
- discord: HTTP 4xx not retried (server rejection)
- webhook: HTTP 4xx not retried
- webhook: success on first attempt (no retry)

</details>

<details><summary>✅ <b>notifiers selfhosted</b> — 42 checks</summary>

The self-hostable notification channels: Apprise, Gotify, Matrix.

- apprise: URL alone is enough
- apprise: title carries BOT_LABEL
- apprise: remote host is labelled
- apprise: local host is not labelled
- apprise: a failure is type=failure
- apprise: a success is type=success
- apprise: stateless targets are passed through
- apprise: stateful sends no urls
- apprise: a failing send never prints the token in APPRISE_URLS
- apprise: …nor the response body it came in
- apprise: …but it still says the send failed, with the status
- apprise: unset → not configured
- gotify: URL without a token is not configured
- gotify: URL + token → configured
- gotify: /message is appended to the base URL
- gotify: token travels in a header
- gotify: a full endpoint URL is not doubled
- gotify: a failure is high priority (bypasses quiet hours)
- gotify: a success is normal priority
- gotify: remote host is labelled
- gotify: token removed → not configured
- matrix: without a room it is not configured
- matrix: homeserver + token + room → configured
- matrix: sends with PUT
- matrix: uses the client v3 send endpoint
- matrix: bearer token in the header
- matrix: each send gets a fresh transaction id
- matrix: room id is URL-escaped
- matrix: plain body is always present
- matrix: HTML variant is offered
- matrix: remote host is labelled
- matrix: a failure is marked in the text
- matrix: a container name is escaped into the HTML body
- matrix: a docker error is escaped too
- matrix: an ampersand in an image ref is escaped
- matrix: the plain body is left alone (it is not HTML)
- matrix: a safe changelog URL is still a link
- matrix: a javascript: changelog URL is never made into an href
- matrix: …and it is still shown, as text
- matrix: a URL that would break out of the attribute is not linked
- matrix: the update LIST escapes names and images too
- matrix: unset → not configured

</details>

<details><summary>✅ <b>ntfy title</b> — 10 checks</summary>

ntfy titles with non-ASCII characters (dc#120).

- ascii is left alone
- empty is safe
- None is safe
- emoji is encoded
- the encoded value is a latin-1 header
- an umlaut is encoded too
- an umlaut is NOT sent raw
- round-trips: 🖥 pve1
- round-trips: Grün Größe
- round-trips: [🖥 pve1] 3 Updates

</details>

<details><summary>✅ <b>send only</b> — 7 checks</summary>

Send-only Telegram mode (#2, @famewolf).

- send-only: never calls getUpdates
- send-only: never calls setMyCommands
- send-only: makes no api calls at all
- send-only: bot still 'enabled' for sending
- default: polling ON
- env false: polling OFF
- env true: polling ON

</details>

<details><summary>✅ <b>smtp</b> — 12 checks</summary>

Test the native e-mail / SMTP notifier (#2).

- has_channels true when configured
- has_channels false when unset
- starttls used
- auth login called
- multi-recipient parsed
- BOT_LABEL prefixes subject
- From set
- ssl: no starttls
- none+no-user: no starttls, no login
- version: old → new arrow
- version: single known
- version: none → empty

</details>

<details><summary>✅ <b>websocket client</b> — 29 checks</summary>

The hand-rolled WebSocket client (RFC 6455), needed for the Discord bot.

- accept key matches the RFC example
- client frames set the mask bit
- masking key makes the payload differ
- masking is reversible
- 125 bytes: 7-bit length
- 126 bytes: 16-bit length
- 70000 bytes: 64-bit length
- FIN is always set outbound
- handshake completes against a real server
- client sends a Sec-WebSocket-Key
- client announces version 13
- client sends the Upgrade header
- receives a whole message
- reassembles a fragmented message
- ping is answered without surfacing
- client answered the ping with a pong
- pong echoes the ping payload
- client message arrives intact
- server close ends the stream
- frame size cap is sane
- a non-upgrading server is refused
- a 500 whose reason phrase contains 101 is refused
- a genuine 101 is still accepted
- a timeout mid-message does not return a partial message
- recv() resumes the message it was in the middle of
- a close still ends the stream
- the close status code is kept
- an endless fragment stream is refused
- …and it gives up near the frame cap, not gigabytes later

</details>

### Web interface

<details><summary>✅ <b>audit</b> — 22 checks</summary>

Who did what, through which front end — and no secrets in the file.

- no secret value survives redaction
- harmless fields are kept
- redaction keeps the length
- an empty secret is marked empty
- an unknown secret-looking key is hidden
- every entry is written
- the front end is recorded
- the actor is recorded
- the target is recorded
- newest first when read back
- no empty detail key
- no empty target key
- the log is capped
- the cap drops the OLDEST
- an unwritable path is survived
- a corrupt file is survived
- an unconfigured log is a no-op
- the web seam is in do_POST
- the web records in exactly one place
- telegram records in one place
- discord records in one place
- the body is replayed to the handlers

</details>

<details><summary>✅ <b>disabled reasons</b> — 4 checks</summary>

A disabled button must say why it is disabled (#55, @LeeNX).

- the row has buttons disabled by monitor-only
- every one of them names the reason
- pin explains monitor-only first
- the label marker keeps its distance

</details>

<details><summary>✅ <b>form nesting</b> — 6 checks</summary>

No nested <form> in the Web UI, and every settings field reaches the form.

- no <form> opens inside another
- settings form declares its id
- every settings field is attached to the form
- the save button is attached to the form
- the handler reads nothing the page cannot send
- the page sends nothing the handler ignores

</details>

<details><summary>✅ <b>icons</b> — 4 checks</summary>

Every icon key the templates use must exist.

- every used icon key is defined
- there are icons at all
- every glyph is wrapped for sizing
- all entries parsed

</details>

<details><summary>✅ <b>link render</b> — 61 checks</summary>

Web UI render test for container repo / changelog links (#52, @LeeNX).

- web UI applies prefer_release_url to auto-detected links
- web UI leaves an explicit link alone
- store javascript:  -> no href in the status table
- store javascript:  -> no 🔗 anchor at all in the table
- store javascript:  -> no href on the detail page
- store javascript:  -> no repo row on the detail page
- store javascript:  -> still visible as form text
- store 'data:text/html;base64,PHNj' -> no anchor
- store 'vbscript:msgbox' -> no anchor
- store '//evil.example/x' -> no anchor
- store 'https://exa mple.com' -> no anchor
- store 'https://x.example/"onclick' -> no anchor
- rejected store value falls through to the OCI label
- valid link -> href in the status table
- valid link -> target=_blank
- valid link -> rel=noopener noreferrer
- valid link -> 🔗 as the visible marker
- valid link -> exactly one anchor in the row
- valid link -> tooltip names the origin
- valid link -> href on the detail page
- detail anchor carries rel=noopener noreferrer
- detail overview row shows the URL as text
- no link at all -> no anchor in the row
- order: docksentry.link beats store + OCI
- order: label origin named in the tooltip
- order: store beats the OCI source label
- order: OCI source beats OCI url
- order: OCI source origin named
- order: OCI url used as fallback
- registry guess is not linked in the status table
- registry guess still resolves for other surfaces
- unsafe docksentry.link falls through to the stored link
- label set -> input disabled
- label set -> save button disabled too
- label set -> 🏷 marker on the section
- label set -> 🏷 uses the existing web_label_authoritative text
- label set -> field shows the EFFECTIVE (label) URL
- label set -> the label URL is what gets linked
- no label -> input usable
- no label -> no 🏷 on the link section
- no label -> field shows the stored URL
- unsafe label -> form stays usable
- /api/link feedback: ?link=saved renders a notice
- /api/link feedback: ?link=cleared renders a notice
- /api/link feedback: ?link=rejected renders a notice
- /api/link feedback: no notice without the query param
- restore: valid entry kept
- restore: javascript: entry dropped
- restore: data: entry dropped
- restore: relative path dropped
- restore: non-string value dropped
- restore: padded valid URL kept, trimmed
- restore: padded javascript: still dropped
- restore: exactly two entries survive
- restore: drop count reported
- restore: drop count visible in the toast text
- restore: drop also listed under errors
- restore: clean bundle keeps everything
- restore: clean bundle reports plain 'links'
- restore: clean bundle drops nothing
- render gate holds independently of the restore filter

</details>

<details><summary>✅ <b>link safety</b> — 39 checks</summary>

Test the shared link validator (#52, @LeeNX).

- reject: javascript:
- reject: JavaScript: (mixed case)
- reject: JAVASCRIPT: (upper)
- reject: leading space before scheme
- reject: leading tab before scheme
- reject: leading newline before scheme
- reject: data:text/html
- reject: vbscript:
- reject: file:
- reject: scheme-relative //evil.example
- reject: http:// without host
- reject: https:/// without host
- reject: bare hostname (no scheme)
- reject: relative path
- reject: 600+ char URL
- accept: exactly at length cap
- reject: one char over cap
- reject: closing paren
- reject: opening paren
- reject: newline inside URL
- reject: carriage return inside URL
- reject: embedded space
- reject: double quote
- reject: single quote
- reject: angle brackets
- reject: backslash
- reject: NUL byte
- reject: backtick
- reject: empty string
- reject: None
- reject: non-string
- reject: whitespace only
- accept: plain https URL
- accept: http URL
- accept: URL with port
- accept: query string with comma
- accept: fragment + encoded space
- accept: uppercase scheme (case-insensitive)
- accept: IP host

</details>

<details><summary>✅ <b>metrics api</b> — 14 checks</summary>

The machine-readable endpoints and the tokens that guard them.

- plain text is untouched
- a quote is escaped
- a backslash is escaped
- a newline is removed
- empty is safe
- None is safe
- backslash then quote
- tokens are compared in constant time
- a wrong token is rejected, not passed on
- only the two read paths accept a token
- the token check is GET-only
- no gauge carries a _total suffix
- the gauge is named plainly
- gathering does not start a check

</details>

<details><summary>✅ <b>settings fields render</b> — 16 checks</summary>

Web UI render test for the five settings that gained a form field.

- password value is NOT rendered anywhere in the HTML
- web_password field is in the General tab
- web_password field is type=password
- web_password field renders empty (value="")
- web_password field has a placeholder
- healthcheck_max_starting field is in the Updates tab
- healthcheck field shows the current value
- docker_stop_timeout field is in the Updates tab
- docker_stop_timeout field shows the current value
- Monitoring heading is in the Notifications tab
- monitor_enabled checkbox is in the Notifications tab
- monitor_enabled reflects the config (checked)
- monitor_interval_seconds field is in the Notifications tab
- monitor_interval shows the current value with a 15s floor
- password field is not on the Updates tab
- monitor fields are not on the Updates tab

</details>
